/*break-> interrompe laços de repeticao, e faz com que o codigo seja seguido linearmente fora do escopo bloco,apos sua ativacao
continue-> faz com que o escopo do bloco seja ignorado dps q ela for ativada,antes de ativada o escopo do bloco segue normal, mas n faz com que saia do loop
for-> sintaxe: for(op inicial(executada 1 vez antes do for comecar);cond de entrada;op de att(executada dps do bloco)){instrucoes do bloco}
switch-> funciona so com cond do tipo expx==constx, e dps que uma condicao for verdadeira ele vai executar todos os blocos abaixo da condicao verdadeira
    sintaxe:switch(expressao){case constante 1: ... case constante x:... default: ...}
      jeito1-> exp1==const1{...} exp2==const2{...} ... expx==constx{...} default(opcional)
      jeito2->calc-switch.c
*/